// This file is run before each test file
// Add any global setup needed for React testing
import '@testing-library/jest-dom';

// Mock global objects if needed
// Example:
// global.fetch = jest.fn();
